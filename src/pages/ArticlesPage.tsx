/** Placeholder da listagem (Fase 8: busca, filtro, grid/lista, cards do Figma). */
export function ArticlesPage() {
  return (
    <div className="mx-auto max-w-6xl px-4 py-10">
      <h1 className="text-3xl font-bold">Artigos</h1>
      <p className="mt-2 text-muted-foreground">
        A listagem completa (busca, filtro e alternancia grid/lista) chega na Fase 8.
      </p>
    </div>
  );
}
